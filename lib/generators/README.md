# Generators (Planned Extraction)

Generators for docs and config (e.g., `.ai-coding-guide.md`, `AGENTS.md`, `.cursorrules`, `eslint.config.js`)
will be extracted into this directory as part of the CLI refactor. The current implementation
resides in `bin/cli.js` with small, pure functions.